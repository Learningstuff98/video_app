<div class="container">
  <div class="box col-10">
    <%= simple_form_for @channel, url: channel_path(@channel) do |f|%>
      <%= f.input :name %>
      <%= f.input :description %>
      <%= f.submit 'Post Edit', class: 'btn btn-primary make-it-green' %>
    <% end %>
  </div>
</div>
